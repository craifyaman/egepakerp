@model StokHareket
<!--begin::Form-->
<form id="StokHareketForm">
    @Html.HiddenFor(x => x.StokHareketId)
    <div class="form-group row">
        @*<div class="col-lg-12">
            @{
                var siparisListe = BaseSiparis().ToList();
            }
            <label>Sipariş:</label>
            <select class="form-control" id="SiparisId" name="SiparisId">
                @foreach (var item in siparisListe)
                {
                    var attr = item.SiparisId == Model?.SiparisId ? "selected" : "";
                    <option @attr value="@(item.SiparisId)">@(item.SiparisAdi)</option>
                }
            </select>
        </div>*@

    </div>

    <div class="form-group row">
        @*<div class="col-lg-6">
            @{
                var siparisKalip = BaseSiparisKalipListe();
            }
            <label>Kalıp:</label>
            <select class="form-control" id="SiparisKalipId" name="SiparisKalipId">
                @foreach (var item in siparisKalip.GroupBy(x=>x.KalipKod))
                {
                    var attr = item.FirstOrDefault().SiparisKalipId == Model?.SiparisKalipId ? "selected" : "";
                    <option @attr value="@(item.FirstOrDefault().SiparisKalipId)">@(item.FirstOrDefault().KalipKod)</option>
                }
            </select>
        </div>*@

        @*<div class="col-lg-12">
            @{
                var aksiyonTypeList = BaseStokHareketType().ToList();
            }
            <label>Hareket Tip:</label>
            <select class="form-control" id="StokHareketTypeId" name="StokHareketTypeId">
                @foreach (var item in aksiyonTypeList)
                {
                    var attr = item.StokHareketTypeId == Model?.StokHareketTypeId ? "selected" : "";
                    <option @attr value="@(item.StokHareketTypeId)">@(item.Type)</option>
                }
            </select>
        </div>*@
    </div>

    <div class="form-group row">
      
        <div class="col-12">
            <label>Adet:</label>
            @Html.TextBoxFor(x => x.Adet, new { @class = "form-control" })
        </div>

        <div class="col-12">
            <label>Yer:</label>
            @Html.TextBoxFor(x => x.Yer, new { @class = "form-control" })
        </div>

        @*<div class="col-6">
            <label>MontajKod:</label>
            @Html.TextBoxFor(x => x.MontajKod, new { @class = "form-control", autocomplete = "off" })
        </div>

        <div class="col-lg-12">
            <label>Montajlı mı:</label>
            @Html.EditorFor(x => x.MontajliMi, new { @class = "form-control" })
        </div>*@
    </div>

</form>
<!--end::Form-->
